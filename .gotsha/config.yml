#autogenerated: true # Remove this line to start using Gotsha

# Git hooks config
post_commit_tests: true
pre_push_tests: true

# Your test commands to run
commands:
# - bundle exec rubocop
# - bundle exec rspec
 - rubocop && rspec

# Print out test commands output
debug: false
