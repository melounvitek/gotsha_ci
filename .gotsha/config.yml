#autogenerated: true # Remove this line to start using Gotsha

# Git hooks config
post_commit_tests: true
pre_push_tests: true

# Your test commands to run
commands:
 - rubocop
 - rspec

# Print out more details (Bash commands output, etc.)
verbose: false
