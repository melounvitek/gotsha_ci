autogenerated: true # Remove this line to start using Gotsha

# Git hooks config
#
# * `status`: print out quick status info (whether last commit is verified or not)
# * `run`: run the tests
post_commit:
  status: true
  run: true

pre_push:
  status: true
  run: false

# Your actual test commands
#
# Example:
#
# ```
# commands:
# - bundle exec rubocop
# - bundle exec rspec
# ```
commands:
 - bundle exec rspec

