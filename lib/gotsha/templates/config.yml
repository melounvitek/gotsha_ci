autogenerated: true # Remove this line to start using Gotsha

# Git hooks config
post_commit_tests: false # run tests for every commit
pre_push_tests: true # run tests on every push

# Your test command(s) (multiple supported, one per line)
commands:
#  - bundle exec rspec
#  - rubocop && rails t
#  - docker exec great-app rspec

# Print out more details (Bash commands output, etc.)
verbose: false
